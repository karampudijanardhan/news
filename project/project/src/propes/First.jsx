const First = (propes) => {
    const{city,name,age}=propes.name
    return ( 
        <div>
            <div>{city}</div>
            <div>{age}</div>
            <div>{name}</div>
            <div>{propes.nam}</div>
        </div>
     );
}
 
export default First;